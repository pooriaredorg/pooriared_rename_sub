name: Generate Subscriptions

on:
  workflow_dispatch:
    inputs:
      base_name_suffix:
        description: 'نامی که می‌خواهید به انتهای نام ساب‌ها و فایل‌ها اضافه شود (مثلاً ALI)'
        required: true
        default: 'suffix'
        type: string
      config_index: 
        description: 'شماره کانفیگی که می‌خواهید به ساب تبدیل شود (مثلاً 1 برای کانفیگ اول)'
        required: true
        default: '1'
        type: string 

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    # 🌟 گام اصلاحی: حذف package.json و نصب ساده‌تر
    - name: Install Dependencies Globally
      run: npm install @octokit/rest node-fetch

    - name: Run Subscriptions Generator Script (Final Attempt)
      id: generate
      # 🌟 اجرای مستقیم فایل با Node (بدون npx و فقط با مسیر نسبی)
      # در این حالت، Action تمام وابستگی‌ها را در مسیر اصلی نصب کرده است.
      run: node .github/workflows/generate.js
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        CONFIG_URL: "https://raw.githubusercontent.com/pooriaredorg/pooriaredorg/refs/heads/main/configs/proxy_configs.txt"
        
        CONFIG_NAME_SUFFIX: ${{ github.event.inputs.base_name_suffix }} 
        CONFIG_INDEX: ${{ github.event.inputs.config_index }}
